import React, { Component } from 'react';
import Axios from 'axios'

class BackgroundImageBuilder extends Component {
  state = {
    backgroundImgArray =[]
  }
  componentDidMount() {
    
  }
  render() {
    return (
      <div>

      </div>
    );
  }
}

BackgroundImageBuilder.propTypes = {

};

export default BackgroundImageBuilder;